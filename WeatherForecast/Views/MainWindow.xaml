<Window x:Class="WeatherForecast.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Weather Forecast" Height="450" Width="800" d:DataContext="{d:SampleData WeatherViewModel (in WeatherForecast.ViewModels)}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="10">
            <TextBlock Text="Current Temperature:" FontWeight="Bold" FontSize="16" />
            <TextBlock Text="{Binding CurrentWeather.Temperature, StringFormat='\{0\}°C'}" FontSize="32" />
            <TextBlock Text="{Binding CurrentWeather.Weather}" FontSize="16" Foreground="Gray" />
        </StackPanel>

        <TextBlock Text="Forecast" FontWeight="Bold" FontSize="16" Margin="10,20,10,10" Grid.Row="1" />

        <ListBox ItemsSource="{Binding Forecast}" Grid.Row="2" Margin="0,10,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Hours}" Margin="0,0,10,0" d:DataContext="{d:SampleData WeatherData (in WeatherForecast.Models)}" />
                            <TextBlock Text="hr:" FontWeight="Bold" Margin="0,0,10,0" />
                            <TextBlock Text="{Binding Temperature, StringFormat='\{0\}°C'}" FontWeight="Bold" Margin="0,0,20,0" d:DataContext="{d:SampleData WeatherData (in WeatherForecast.Models)}" />
                            <TextBlock Text="{Binding Weather}" d:DataContext="{d:SampleData WeatherData (in WeatherForecast.Models)}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
